spring:
  application:
    name: produc
  cloud:
    config:
      profile: dev
      discovery:
        enabled: true
        service-id: CONFIG

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/

server:
  port: 8082